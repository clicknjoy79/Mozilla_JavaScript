var foo = 42;    // foo is now a Number
var foo = 'bar'; // foo is now a String
var foo = true;  // foo is now a Boolean
console.log(foo, typeof foo);

// The latest ECMAScript standard defines seven data types:
// Boolean, Null, Undefined, Number, String, Symbol(new in ECMAScript 6), Object

// Primitive values
// All types except objects define immutable values

// 자바 스크립트에서 Number 는 오로지
// double-precision 64-bit binary format IEEE 754 value 로만 존재한다.

// The MIN_VALUE property is the number closest to 0, not the most negative number, that JavaScript can represent.
console.log(Number.MAX_VALUE);
console.log(Number.MIN_VALUE);

// 별 내용이 없다..



